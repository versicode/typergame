import serverConfig from '../config/serverConfig'

export default class World {
    constructor(map, playerRepository) {
        this.map = map
        this.playerRepository = playerRepository
    }


}
